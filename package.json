{
  "name": "eoty-platform",
  "version": "1.0.0",
  "description": "EOTY Platform - Education On The Year. Deploy from project root. Vercel is configured with rootDirectory: 'frontend'",
  "scripts": {
    "deploy": "vercel --prod",
    "deploy:preview": "vercel",
    "deploy:windows": "powershell -ExecutionPolicy Bypass -File ./deploy-vercel.ps1",
    "deploy:unix": "bash ./deploy-vercel.sh"
  },
  "private": true,
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "passport": "^0.7.0",
    "passport-facebook": "^3.0.0",
    "passport-google-oauth20": "^2.0.0",
    "passport-jwt": "^4.0.1"
  }
}
